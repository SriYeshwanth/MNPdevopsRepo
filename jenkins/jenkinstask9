
pipeline {
    agent any
    stages {
        stage('this one original for calling') {
            steps {
               script{
             def srivar="calling success"
                  sh '''
                    echo "the calling is fixed ${srivar}"
                    echo ${srivar}
                    '''            
}
        }
    }
}
}
